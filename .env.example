# Optional. When omitted, reports use Lexar if mounted, otherwise ./reports.
FIXED_INCOME_OUTPUT_DIR=/Volumes/Lexar_Spike/项目归档/china-fixed-income-lab/reports

# Optional OpenAI-compatible endpoint for news classification.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
